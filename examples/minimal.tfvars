# Minimal Example - Only required variables
# Use this for a basic AKS cluster with default settings

prefix   = "myapp"
location = "eastus"
env      = "dev"

# No additional node pools - just the default system node pool
node_pools = {}
